{
  "name": "socketty",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @socketty/web dev",
    "build": "pnpm --filter @socketty/web build",
    "start": "pnpm --filter @socketty/web start",
    "lint": "pnpm --filter @socketty/web lint"
  }
}
